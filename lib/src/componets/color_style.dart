import 'package:flutter/material.dart';

class ColorStyle {
  static const primary = Color.fromRGBO(255, 165, 0, 0.85);
  static const secundary = Color.fromRGBO(234, 30, 45, 1.0);
  static const background = Colors.black;
  static const text = Color.fromRGBO(255, 165, 0, 0.85);
  static const field = Color.fromRGBO(222, 234, 249, 1);
}